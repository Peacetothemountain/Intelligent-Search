<?xml version="1.0" encoding="utf-8"?>
<!--
    Native Pixel Launcher QSB background - Android 16 QPR1 Beta 2 style.
    Single unified pill: uses ?attr/colorSurfaceContainer which is the
    exact token Pixel Launcher uses for its built-in search bar background.
    This is a slightly elevated surface — NOT black, NOT purple.
    On a dark wallpaper it appears as a dark-grey translucent capsule.
-->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@android:color/system_neutral1_800" />
    <corners android:radius="50dp" />
</shape>
